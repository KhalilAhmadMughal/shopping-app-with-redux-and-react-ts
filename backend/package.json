{
  "dependencies": {
    "json-server": "^0.17.2"
  },
  "name": "json-server-api",
  "version": "1.0.0",
  "description": "Fake Server",
  "main": "main.js",
  "scripts": {
    "start": "json-server --watch ./db.json --port 5000"
  }
}
